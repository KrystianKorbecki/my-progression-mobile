name: my_progression_mobile
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.13.0 <3.0.0"

dependencies:
  date_field: ^2.0.1
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  json_annotation: ^4.0.1
  #database:
  sqflite: ^2.0.0+3

  #format: DateTime
  intl: ^0.17.0
  #Popup
  rflutter_alert: ^2.0.4
  flutter_svg: ^0.22.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  json_serializable: ^4.1.4
  build_runner: ^2.1.1



flutter:
  uses-material-design: true
  assets:
    - resources/database/
    - resources/icons/